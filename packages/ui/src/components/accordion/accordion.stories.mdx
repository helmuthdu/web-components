import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import './accordion';
import './accordion-group';

# Accordion

<Meta title="Components/Accordion" decorators={[story => `<div class="canvas">${story()}</div>`]} />

## Playground

<Canvas withToolbar={true}>
  <Story
    name="Playground"
    argTypes={{
      variant: {
        name: 'variant',
        type: { name: 'string', required: true },
        defaultValue: 'primary',
        control: { type: 'select' },
        options: ['primary', 'secondary', 'tertiary']
      }
    }}>
    {({ variant }) => /*html*/ `
      <style>
         .accordion {
           width: var(--size-96);
         }
      </style>
      <ui-accordion-group class="accordion" data-variant="${variant}">
        <ui-accordion>
          <h2 slot="header">1 item</h2>
          <p>Laborum elit sint velit nulla aliqua sint anim id et adipisicing dolore.</p>
        </ui-accordion>
        <ui-accordion>
          <h2 slot="header">2 item</h2>
          <p>Laborum elit sint velit nulla aliqua sint anim id et adipisicing dolore.</p>
        </ui-accordion>
        <ui-accordion>
          <h2 slot="header">3 item</h2>
          <p>Laborum elit sint velit nulla aliqua sint anim id et adipisicing dolore.</p>
        </ui-accordion>
      </ui-accordion-group>
    `}
  </Story>
</Canvas>

#### SLOT & PROPS

<ArgsTable story="Playground" />
