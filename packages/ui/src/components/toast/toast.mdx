import { ArgsTable, Canvas, Meta, Story } from '@storybook/blocks';
import './toast';
import * as ToastStories from './toast.stories';

# Toast

<Meta of={ToastStories} />

export const truncate = (text, size = 127) => text.slice(0, size) + (text.length > size ? '…' : '');

## Examples

<Canvas withToolbar={true}>
  <Story of={ToastStories.Examples} />
</Canvas>

## Playground

<Canvas withToolbar={true}>
  <Story of={ToastStories.Playground} />
</Canvas>

#### SLOT & PROPS

<ArgsTable story="Playground" />
