import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import './box';

# Box

<Meta title="Components/Box" decorators={[story => `<div class="canvas">${story()}</div>`]} />

## Playground

<Canvas withToolbar={true}>
  <Story
    name="Playground"
    argTypes={{
      slot: {
        type: { name: 'string', required: true }
      }
    }}
    args={{
      slot: 'Get Started'
    }}>
    {({ slot, ...props }) => /*html*/ `
      <ui-box>
        ${slot}
      </ui-box>
    `}
  </Story>
</Canvas>

#### SLOT & PROPS

<ArgsTable story="Playground" />
