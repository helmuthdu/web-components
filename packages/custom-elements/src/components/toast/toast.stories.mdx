import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { truncate } from '../../lib/create-element';
import './toast';

# Toast

<Meta title="Components/Toast" />

## Examples

<Canvas withToolbar={true}>
  <Story
    name="Examples"
    decorators={[story => `<div class="flex items-center justify-center flex-wrap gap-4">${story()}</div>`]}>
    {() => /*html*/ `
      <ui-toast data-fixed>
        <header slot="header">My Notification</header>
        <span slot="meta">10 minutes ago</span>
        Excepteur est ullamco do sit culpa laboris.
      </ui-toast>
      <ui-toast data-variant="contrast" data-fixed>
        <header slot="header">My Notification</header>
        Excepteur est ullamco do sit culpa laboris.
      </ui-toast>
      <ui-toast data-variant="info" data-fixed>
        ${truncate('Ut minim voluptate ea laboris velit duis exercitation sit minim ex magna commodo id.', 78)}
      </ui-toast>
      <ui-toast data-variant="success" data-fixed>
        Consectetur mollit exercitation duis consectetur pariatur cupidatat consequat.
      </ui-toast>
      <ui-toast data-variant="error" data-fixed>
        Duis amet cupidatat voluptate dolore nostrud ad fugiat reprehenderit.
      </ui-toast>`}
  </Story>
</Canvas>

## Playground

<Canvas withToolbar={true}>
  <Story
    name="Playground"
    decorators={[story => `<div class="flex items-center justify-center w-full p-10">${story()}</div>`]}
    args={{
      slot: /*html*/ `<header slot="header">My Notification</header><span slot="meta">10 minutes ago</span>Excepteur est ullamco do sit culpa laboris.`,
      variant: 'info'
    }}
    argTypes={{
      variant: {
        name: 'data-variant',
        type: { name: 'string', required: true },
        control: {
          type: 'select',
          labels: {
            [undefined]: 'none',
            info: 'info',
            error: 'error',
            success: 'success',
            contrast: 'contrast'
          }
        },
        options: [undefined, 'info', 'error', 'success', 'contrast']
      }
    }}>
    {({ slot, ...props }) => /*html*/ `
    <ui-toast
      ${props.variant ? `data-variant="${props.variant}"` : ''}
      data-fixed>
      ${slot}
    </ui-toast>
  `}
  </Story>
</Canvas>

#### SLOT & PROPS

<ArgsTable story="Playground" />
