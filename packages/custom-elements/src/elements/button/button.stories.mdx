import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import './button';
import './button-group';

# Button

## Example

<Meta title="Elements/Button" />

<Canvas withToolbar={true}>
  <Story
    name="Example"
    decorators={[story => `<div class="flex items-center justify-center flex-wrap gap-4">${story()}</div>`]}>
    {() => /*html*/ `
      <ui-button>Get Started</ui-button>
      <ui-button data-variant="error"> Get Started</ui-button>
      <ui-button data-variant="success">Get Started</ui-button>
      <ui-button data-outline>Get Started</ui-button>
      <ui-button data-variant="error" data-outline> Get Started</ui-button>
      <ui-button data-variant="success" data-outline>Get Started</ui-button>
      <ui-button data-loading data-disabled>Get Started</ui-button>
      <ui-button data-circle data-variant="success">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path></svg>
      </ui-button>
      <ui-button data-circle data-variant="error">
        UI
      </ui-button>
      <ui-button data-circle data-loading>
        UI
      </ui-button>
      <ui-button data-circle data-outline>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path></svg>
      </ui-button>
      <ui-button-group>
        <ui-button>Get Started</ui-button>
        <ui-button data-outline>Get Started</ui-button>
      </ui-button-group>`}
  </Story>
</Canvas>

## Playground

<Canvas withToolbar={true}>
  <Story
    name="Playground"
    decorators={[story => `<div class="flex items-center justify-center w-full p-10">${story()}</div>`]}
    args={{
      slot: 'Get Started',
      variant: 'primary',
      size: 'md',
      block: false,
      circle: false,
      disabled: false,
      loading: false,
      outline: false,
      rounded: false
    }}
    argTypes={{
      slot: {
        type: { name: 'string', required: true }
      },
      variant: {
        name: 'data-variant',
        control: { type: 'select' },
        options: ['primary', 'error', 'success']
      },
      size: {
        name: 'data-size',
        type: { name: 'string' },
        control: { type: 'select' },
        options: ['xs', 'sm', 'md', 'lg', 'xl']
      },
      block: {
        name: 'data-block',
        type: { name: 'boolean' }
      },
      circle: {
        name: 'data-circle',
        type: { name: 'boolean' }
      },
      disabled: {
        name: 'data-disabled',
        type: { name: 'boolean' }
      },
      loading: {
        name: 'data-loading',
        type: { name: 'boolean' }
      },
      outline: {
        name: 'data-outline',
        type: { name: 'boolean' }
      },
      rounded: {
        name: 'data-rounded',
        type: { name: 'boolean' }
      }
    }}>
    {({ slot, ...props }) => /*html*/ `
    <ui-button
      ${props.variant ? `data-variant="${props.variant}"` : ''}
      ${props.size ? `data-size="${props.size}"` : ''}
      ${props.disabled ? 'data-disabled' : ''}
      ${props.loading ? 'data-loading' : ''}
      ${props.outline ? 'data-outline' : ''}
      ${props.circle ? 'data-circle' : ''}
      ${props.rounded ? 'data-rounded' : ''}
      ${props.block ? 'data-block' : ''}
      >
        ${slot}
      </ui-button>
    `}
  </Story>
</Canvas>

### SLOT & PROPS

<ArgsTable story="Playground" />
